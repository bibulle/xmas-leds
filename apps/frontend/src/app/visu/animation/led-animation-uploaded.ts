import { LedAnimationAbstract } from './led-animation-abstract';

export class LedAnimationUploaded extends LedAnimationAbstract {
  /**
   * Calculate the animations (do not exists)
   */
  calculate = undefined;
}
